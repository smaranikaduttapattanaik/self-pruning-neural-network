# self-pruning-neural-network
Self-pruning neural network implementation using PyTorch on CIFAR-10.  Implements learnable gating mechanism with L1 regularization to achieve dynamic sparsity.  Analyzes trade-off between model accuracy and sparsity for different lambda values.
